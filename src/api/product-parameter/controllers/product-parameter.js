'use strict';

/**
 * product-parameter controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::product-parameter.product-parameter');
